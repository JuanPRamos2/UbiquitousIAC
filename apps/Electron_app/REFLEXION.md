# Reflexión

Escribe aquí una reflexión corta de por qué Electron consume el XML del microservicio Flask, por qué el endpoint se guarda en LocalStorage y por qué las portadas salen de `/covers` y `/books-images`.
