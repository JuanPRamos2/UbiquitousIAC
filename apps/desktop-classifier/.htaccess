# Código Java de laboratorio: no ejecutarlo desde Apache.
# El reporte enlaza el tar.gz en descargas/.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
