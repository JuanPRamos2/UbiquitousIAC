# Copie este archivo a .env si no existe.
# Valores de demostración alineados con docker-compose.yml (no usar en producción).

NODE_ENV=development
HOST=0.0.0.0
PORT=3000
BASE_PATH=/library

DB_HOST=127.0.0.1
DB_PORT=5433
DB_NAME=library_db
DB_USER=library_user
DB_PASSWORD=666

SESSION_SECRET=libreria-demo-session-610248
