# No servir el módulo Flask ni secretos desde Apache/ubiquitous.
# Sin "Options": esa directiva provoca HTTP 500 en el userdir de UDEM.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
