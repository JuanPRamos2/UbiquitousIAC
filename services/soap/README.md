# Microservicio SOAP + JSON

Ruta: `services/soap/` (no usar `app/`).

```bash
# desde la raíz de UbiquitousIAC
./run-flask.sh
```

Sin `format` → XML. Con `?format=json` → JSON.
`POST /soap` → Envelope SOAP.
